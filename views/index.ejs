<% if (!locals.user) { %>
	<div class="hero-unit">
		<h1>janeze</h1>
		<p>my first nodejs work</p>
		<p>
			<a class="btn btn-primary btn-large" href="/login">登录</a>
			<a class="btn btn-large" href="/reg">立即注册</a>
		</p>
	</div>
<% } else { %>
			<%- partial('say') %>
<% } %>
<%- partial('posts',{posts:posts}) %>
